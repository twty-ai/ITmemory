# 2026-03-21 — Work Log

## Tasks Completed

1. **Audit Columns Added** — Added By, Modified By, Added Time, Modified Time on Team Dispatch All Orders and main Orders page
2. **Stop Label Fix** — P0/D2 → P1/D1 (use `stop.stopType` directly instead of reconstructing)
3. **Active Board Error Fix** — `modifiedBy` column missing from DB, ran `prisma db push`
4. **Split Date/Time in Trip Activities** — Separate DATE and TIME columns in activity table
5. **Hide Empty Activities Header** — Clean "No activities" message when no activities
6. **Status Filter Race Condition Fix** — Removed `setTimeout()` that captured stale closure state
7. **Default Status Filter to Active** — All tabs (orders, stops, trips) default to Active filter
8. **Highway Trips/Stops Fix** — Applied business rules (NOT PENSKE/CANADIAN TIRE/RYDER, NOT LOCAL) instead of `dispatchTeam` column
9. **Test Record Deletion** — Removed TEST ROHAN1 orders, trips TR-2026-00001 through 00007
10. **Order Detail API Fix** — Prisma Decimal serialization with `.toNumber()` replacer
11. **Order Detail API v2** — Replaced `include: true` with explicit `select` to prevent Vercel cache mismatch
12. **Zoho Sync Fix** — Manual sync now resolves customer before creating orders (was failing on 169 records)
13. **Zoho Sync Error Tracking** — Added `errorDetails` array to sync log metadata
14. **dispatchTeam Backfill** — Backfilled 393 orders with NULL dispatchTeam
15. **SHUTTLES CANADIAN TIRE Fix** — Moved to CITY team (not CANADIAN_TIRE), updated customer primaryTeam
16. **Order Sorting** — Changed from `createdAt desc` to `autoNumber desc` for proper numeric sorting
17. **Report Config Sorting** — Added Save & Apply button, server-side sort support via query params

## Key Architecture Decisions
- HIGHWAY_US team uses business rules (NOT clauses) NOT `dispatchTeam` column — matches Zoho migration pattern
- Prisma Decimal objects need explicit `.toNumber()` conversion for `NextResponse.json()`
- Use explicit `select` instead of `include: true` on Prisma relations to avoid Vercel build cache issues
- SHUTTLES CANADIAN TIRE is a separate business entity from CANADIAN TIRE — classified as CITY team

## Database Changes
- Added `modifiedBy String?` to Order model
- Backfilled 393 orders with `dispatchTeam` based on customer/direction rules
- Set SHUTTLES CANADIAN TIRE customers' `primaryTeam` to NULL
